return {
  "williamboman/mason.nvim",
  cmd = "Mason",
  opts = {
    ui = {
      border = vim.g.border,
      backdrop = vim.g.backdrop,
      height = vim.g.height,
      width = vim.g.width,
    },
  },
}
